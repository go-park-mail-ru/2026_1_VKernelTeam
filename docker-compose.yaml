services:
  clover:
    container_name: clover
    build:
      context: .
      dockerfile: ./Dockerfile
    env_file:
      - .env
    restart: always
    ports:
      - 8000:8000
    volumes:
      # путь_на_хосте : путь_в_контейнере
      - ./static:/app/static
      - ./storage:/app/storage
